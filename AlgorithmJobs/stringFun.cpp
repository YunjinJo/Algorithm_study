#include<stdio.h>
#include<string>
#include<iostream>
using namespace std;


int main(){
    string myString;
    cout << (myString.length()); // X의 길이 반환
    
    //strcmp(A, B); // A와 B가 같은지 판단
    //strcpy(A, B); // B를 A에 복사
}